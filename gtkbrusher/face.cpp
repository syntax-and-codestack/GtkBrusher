#include "face.h"
#include "plane.h"

#define FACE_DETAIL 0000x80000

face_t * Planes_MakeFace(plane_t * p1, plane_t * p2, face_t * face){
            if( q_eBrushPrimitMode ){
                 p1 = plane(p1->pvec, p1->point);
                 p2 = plane(p2->pvec, p2->point);\

            for( p1; p2; face ){
                face = Face_MakeVectors();
            }

            }
};